# Core web framework
flask==2.3.0
werkzeug==2.3.0

# Production WSGI server (required for Render deployment)
gunicorn==21.2.0

# Machine learning / model
tensorflow-cpu>=2.12,<2.16
numpy==1.24.3
scikit-learn==1.3.0

# Image and video processing
# NOTE: opencv-python-headless is used instead of opencv-python
# because cloud servers have no display — the headless version skips GUI dependencies
opencv-python-headless==4.8.0.74
pillow==10.0.0
h5py>=3.8.0

# Training utilities (charts, reports)
matplotlib==3.7.1
seaborn==0.12.2

# NOTE: pyttsx3 removed — it requires system audio/speaker drivers that don't
# exist on cloud servers. Speech is now handled by the browser's Web Speech API instead.
